<UserControl
    x:Class="Revit.AI.Assistant.UI.AIPanelUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:Revit.AI.Assistant.UIComponents"
    xmlns:converters="clr-namespace:Revit.AI.Assistant.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Revit.AI.Assistant.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <UserControl.DataContext>
        <local:AIPanelUserControlDataContext />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BooleanToVisibilityHidden x:Key="BooleanToVisibilityHidden" />
            <Style TargetType="Button">
                <Setter Property="Background" Value="Black" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1B64C1" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#134D99" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <components:LoadingSpinner
            Grid.Row="0"
            Grid.RowSpan="2"
            Panel.ZIndex="1"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityHidden}}" />

        <ScrollViewer
            Grid.Row="0"
            Margin="5"
            VerticalScrollBarVisibility="Auto">
            <TextBox
                Background="White"
                IsReadOnly="True"
                Text="{Binding ChatHistory, Mode=OneWay}"
                TextWrapping="Wrap" />
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox FontSize="14" Text="{Binding Message, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SendMessage}" />
                </TextBox.InputBindings>
            </TextBox>
            <ComboBox
                Grid.Column="2"
                Width="150"
                FontSize="14"
                ItemsSource="{Binding LanguageModels, UpdateSourceTrigger=PropertyChanged}"
                SelectedItem="{Binding LanguageModel, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
            <Button
                Grid.Column="4"
                Command="{Binding SendMessage}"
                Content="Send" />
        </Grid>
    </Grid>
</UserControl>
